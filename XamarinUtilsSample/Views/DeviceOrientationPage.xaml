<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinUtilsSample.Views.DeviceOrientationPage"
             NavigationPage.HasNavigationBar="True">

    <ContentPage.Content>
        <StackLayout Margin="20,35,20,20">
            <Label Text="{Binding TitleText}"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" FontSize="Large" />
            <Button Text="{Binding ButtonText}" Command="{Binding DeviceOrientationCommand}"
                     />
            <Label Text="{Binding LabelText}" FontSize="30" FontAttributes="Bold" TextColor="Black" HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>